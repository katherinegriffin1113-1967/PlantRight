[build]
  command = "npm run build"
  publish = "dist"

# Single-page-app fallback so client-side routes (/login, /app) work on
# direct load and refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
